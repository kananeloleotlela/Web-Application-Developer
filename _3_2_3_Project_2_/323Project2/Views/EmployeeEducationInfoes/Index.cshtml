@model IEnumerable<_323Project2.Models.EmployeeEducationInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EducationCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EducationCodeNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeNumberNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EducationCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EducationCodeNavigation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeNumberNavigation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
