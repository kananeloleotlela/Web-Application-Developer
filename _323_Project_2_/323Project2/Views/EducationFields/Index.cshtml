@model IEnumerable<_323Project2.Models.EducationField>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EducationDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EducationDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EducationCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EducationCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EducationCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
